# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# amplify (web — keep generated outputs out of web root)
.amplify
amplify_outputs*
amplifyconfiguration*

# iOS — amplify_outputs.json is bundled into the app and must be tracked
!test/amplify_outputs.json

# iOS — Xcode user-specific / derived files
xcuserdata/
*.xcuserstate
DerivedData/
*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist

# Old Xcode project (renamed to WashingtonPark.xcodeproj)
test.xcodeproj/
